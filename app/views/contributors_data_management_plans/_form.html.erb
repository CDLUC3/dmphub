<% roles = ContributorDataManagementPlan.roles.keys
     .select { |k| k != 'primary_contact' }
     .map { |k| [humanize_underscored(name: k), k] }
     .sort { |a, b| a[0]<=>b[0] } %>

<%= form.fields_for :contributor_data_management_plans, ContributorDataManagementPlan.new do |pdmp_form| %>
  <%= pdmp_form.label :role %>
  <%= pdmp_form.select :role, roles %>
  <%= render partial: 'contributors/form', locals: { form: pdmp_form, rel: 'other' } %>
  <%= pdmp_form.button 'X', class: 'remove-association', type: 'button' %>
<% end %>
