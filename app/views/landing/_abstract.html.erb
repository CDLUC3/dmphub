<%# locals: project, dmp %>

<ul class="landing-list">
  <li>
    <% if dmp.description.present? || project.description.present? %>
      <%= dmp.description.present? ? sanitize(dmp.description) : sanitize(project.description) %>
    <% else %>
      No abstract was provided.<br>
      Please refer to the 'Is Metadata For' identifier above for more information about this DMP.
    <% end %>
  </li>
</ul>
