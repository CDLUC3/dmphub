<!DOCTYPE html>
<html lang="en">
  <head>
    <title>DMPHub</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag asset_path('favicon.ico') %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <div class="t-step">
      <%# Commented out the header because we are not yet ready for the hub to have a search/dashboard %>
      <%#= render partial: 'layouts/header' %>

      <main>
        <div class="notifications">
          <% if flash[:alert].present? %>
            <span class="notification-alert"><%= flash[:alert] %></span>
          <% end %>
          <% if flash[:notice].present? %>
            <span class="notification-notice"><%= flash[:alert] %></span>
          <% end %>
        </div>

        <%= yield %>

        <span class="spinner"><%#= image_tag 'spinner.jpg', alt: "Please wait spinner" %></span>
      </main>

      <%= render partial: 'layouts/footer' %>
    </div>

  </body>
</html>
