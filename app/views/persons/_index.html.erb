<ul class="other-contacts">
  <% persons.each do |person| %>
    <strong><%= person.role.humanize %></strong>: <%= person.name %> (<%= person.email %>) <%= link_to identifier_to_link(person.orcid), identifier_to_link(person.orcid) %> (X)
  <% end %>
</ul>
