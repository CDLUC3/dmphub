<%
content_for :form_content do
  render partial: '/security_privacy_statements/test_form',
         locals: { statement: @statement }
end

payload = escape_javascript(
  render partial: '/shared/form_wrapper',
         locals: {
           model: @statement,
           path: security_privacy_statements_path,
           method: :post
         })
%>

$('form').replaceWith("<%= payload %>");

<%= render partial: '/shared/response_errors' %>
